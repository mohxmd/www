---
import type { HTMLAttributes } from "astro/types";
import { cn } from "#/lib/utils/cn";

type Props = HTMLAttributes<"div">;

const { class: className, ...props } = Astro.props;
---

<div {...props} class={cn("flex h-16 items-center px-6 pt-6", className)}>
  <slot />
</div>
